/**
 * \file
 * \brief Math functions.
 */

#ifndef UTILS_MATH_H
#define UTILS_MATH_H

/**
 * \brief Maximum value.
 */
#define MATH_MAX(X, Y) (((X) > (Y)) ? (X) : (Y))

/**
 * \brief Minimum value.
 */
#define MATH_MIN(X, Y) (((X) < (Y)) ? (X) : (Y))

#endif

